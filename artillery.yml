config:
  target: 'http://localhost:7070'
  phases:
    - duration: 60
      arrivalRate: 5
scenarios:
  - name: 'proxy test'
    flow:
    - loop:
      - get:
          url: '/?{{ $randomNumber(1,1000000) }}'
      count: 2

          